name: Disabled - Azure Static Web Apps Deploy (optional)

on:
  workflow_dispatch:

jobs:
  disabled-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "Azure Static Web Apps deployment is disabled in this repository."
          echo "If you need to enable it in future, restore .github/workflows/deploy-static-prototype.yml with the appropriate secrets."
