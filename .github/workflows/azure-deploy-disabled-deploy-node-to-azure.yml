name: Disabled - Deploy Node backend to Azure App Service (optional)

on:
  workflow_dispatch:

jobs:
  disabled-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "Azure deployment workflows are disabled in this repository."
          echo "If you need Azure deployment in the future, restore .github/workflows/deploy-node-to-azure.yml with the appropriate secrets."
